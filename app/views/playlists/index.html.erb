<h1>All Playlists</h1>

<% @playlists.each do |playlist| %>
  <!-- this conditional will only show playlists created by the user -->
  <% if playlist.user_id == session[:user_id] %>
  <ul>
    <li><%= link_to playlist.title, playlist %></li>
  </ul>
  <% end %>
<% end %>
